const express = require('express'),
  router = express.Router();

router.get('/', (req, res) => {
  con};
  result.empId = 'EMP-123';
  result.empName = 'John';
  res.status(200});
});

module.exports = router;

